/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["opengraph.githubassets.com", "assets.leetcode.com"],
  },
};

export default nextConfig;
